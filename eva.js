import { mapping, light as lightTheme } from "@eva-design/eva";

export const evaTheme = {
  ...lightTheme,
  "text-font-family": "SomeFont", // Ganti dengan nama font yang ingin Anda gunakan
  // Tambahkan konfigurasi tema khusus UI Kitten Anda di sini
};

export const evaMapping = mapping;
